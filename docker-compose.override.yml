services:
    backend:
        command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
        volumes:
            - ./backend/app:/app
            
    frontend:
        volumes:
            - ./frontend:/app
